echo "	In order to know the provider slash16.org
	or any other site you need to know its ipaddress,
	then use the command:
	whois [IP] | grep \"OrgName\"
	or you can check your result with other resources,
	for example using the command:
	curl ipinfo.io/[IP]\n
	use \"name.com +short\" for getting IP!"